# Redirect totul către index.php (dacă nu e fișier real)
RewriteEngine On

# Servește fișierele existente (CSS/JS/img)
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Restul prin front controller
RewriteRule ^ index.php [L]
